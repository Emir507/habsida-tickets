// eslint-disable-next-line
function Header({ style }) {
  // TODO: use prop types
  return (
    <div style={style}>
      <img src="/main-logo.svg" alt="website logo" />
    </div>
  );
}

export default Header;
